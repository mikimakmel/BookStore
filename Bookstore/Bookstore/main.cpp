#include <iostream>
#include "Menu.h"

int main(int argc, char *argv[])
{
	//static Book *bookPtr;
	//bookPtr = Book::getBookById(100);

	//Book newBook(Book::getBookById(100));
	//Book newBook2(Book::getBookById(101));
	/*newBook.printBook();*/

	menu();
}